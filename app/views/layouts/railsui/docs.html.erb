<!DOCTYPE html>
<html class="h-full antialiased">
  <head>
    <title>
      <% if content_for?(:title ) %>
        <%= yield :title %>
      <% else %>
        Rails UI
      <% end %>
    </title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= favicon_link_tag "favicon.svg" %>
    <%= javascript_include_tag "railsui/application", "application" %>
    <%= javascript_include_tag "https://unpkg.com/alpinejs@3.10.3/dist/cdn.min.js", defer: true %>
    <%= stylesheet_link_tag "railsui/application", "application" %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code&display=swap" rel="stylesheet">

    <%= yield :head %>
  </head>

  <body class="font-normal leading-normal text-slate-800 lg:h-screen relative selection:bg-indigo-50/50 <%= content_for?(:bg_theme).present? ? yield(:bg_theme) : "bg-white dark:bg-slate-900 dark:text-white" %>" data-controller="toggle scroll smooth">
    <%= render "railsui/shared/flash" %>
    <%= render "layouts/railsui/masthead" %>

    <div class="mx-auto lg:py-32 px-4 py-8 max-w-6xl">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-10">
        <div class="col-span-3 relative">
          <%= render "railsui/docs/nav" %>
        </div>
        <div class="col-span-9">
          <%= yield %>
        </div>
      </div>

      <%= yield :demo_scripts %>
    </body>
  </html>
