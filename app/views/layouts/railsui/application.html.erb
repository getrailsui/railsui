<!DOCTYPE html>
<html class="tw-h-full tw-antialiased">
  <head>
    <title>
      <% if content_for?(:title ) %>
        <%= yield :title %>
      <% else %>
        Rails UI
      <% end %>
    </title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= favicon_link_tag "favicon.svg" %>
    <%= javascript_include_tag "railsui/application", "application" %>
    <%= javascript_include_tag "https://unpkg.com/alpinejs@3.10.3/dist/cdn.min.js", defer: true %>

    <%= stylesheet_link_tag "railsui/application", "application" %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code&display=swap" rel="stylesheet">

    <%= yield :head %>
  </head>

  <body class="tw-font-normal tw-leading-normal dark:tw-bg-slate-900 dark:tw-text-white tw-bg-white tw-text-slate-800 tw-h-screen">
    <div class="tw-flex-1 tw-w-full tw-overflow-hidden">
      <%= render "railsui/shared/flash" %>
    </div>
    <div class="tw-min-h-full tw-flex">
      <div class="tw-flex tw-flex-col tw-w-[238px] tw-p-4 tw-border-r dark:tw-border-slate-100 tw-bg-slate-50 tw-overflow-y-auto tw-min-h-full">
        <div class="tw-flex-1">
          <div class="tw-sticky tw-top-1 tw-flex-1">
            <%= link_to  main_app.root_path, class: "tw-flex tw-border tw-bg-white tw-w-8 tw-h-8 tw-items-center tw-justify-center tw-rounded-lg hover:tw-shadow-sm tw-group hover:tw-shadow-indigo tw-mb-4", data: {tippy_content: "Back to main app", controller: "tooltip"} do %>
              <div class="tw-flex tw-items-center  tw-text-sm">
                <%= render_svg "chevron-left", styles: "tw-stroke-current tw-text-slate-500 group-hover:tw-text-indigo-400 tw-w-4 tw-h-4" %>
              </div>
            <% end %>

            <%= render "railsui/shared/nav" %>
          </div>
        </div>
      </div>

      <div class="tw-flex-1 tw-mx-auto lg:tw-py-16 tw-px-4 <%= content_for?(:fullwidth).present? ? "tw-w-full" : "tw-max-w-4xl" %>">
        <%= yield %>
      </div>
    </div>

    <%= yield :demo_scripts %>
  </body>
</html>
