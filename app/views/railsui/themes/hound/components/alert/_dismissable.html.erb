<div id="alert-dismissable">
<%= render layout: example, locals: { heading: "Dismissable", section: "Alert" } do %>

<%= render preview 'gray' do %>
<div class="relative rounded p-4 text-sm flex flex-col gap-2 md:flex-row md:justify-between md:items-start bg-indigo-50/90 dark:bg-indigo-500/10 text-indigo-700 dark:text-indigo-50 dark:border dark:border-indigo-400/30">
  <div class="flex-1">
    <p class="font-semibold mb-1">A dismissible notice</p>
    <div>Your account settings have been successfully updated. The changes will take effect immediately.</div>
  </div>
  <button type="button" class="absolute top-2 right-2 size-8 rounded-full transition ease-in-out duration-200 hover:bg-current/5 dark:hover:bg-current/20 flex items-center justify-center shrink-0">
    <%= icon "x-mark", class: "size-5 text-indigo-500 dark:text-indigo-400 shrink-0", title: "Dismiss" %>
  </button>
</div>
<% end %>

<% content_for :example, flush: true do %>
<% content_for :html, flush: true do %>
<div class="relative rounded p-4 text-sm flex flex-col gap-2 md:flex-row md:justify-between md:items-start bg-indigo-50/90 dark:bg-indigo-500/10 text-indigo-700 dark:text-indigo-50 dark:border dark:border-indigo-400/30">
  <div class="flex-1">
    <p class="font-semibold mb-1">A dismissible notice</p>
    <div>Your account settings have been successfully updated. The changes will take effect immediately.</div>
  </div>
  <button type="button" class="absolute top-2 right-2 size-8 rounded-full transition ease-in-out duration-200 hover:bg-current/5 dark:hover:bg-current/20 flex items-center justify-center shrink-0">
    <%%= icon "x-mark", class: "size-5 text-indigo-500 dark:text-indigo-400 shrink-0", title: "Dismiss" %>
  </button>
</div>
<% end %>

<% content_for :erb, flush: true do %>
<%%= rui :alert, color: :indigo, title: "A dismissable notice", content: "Your account settings have been successfully updated. The changes will take effect immediately.", dismissable: true %>
<% end %>

<% content_for :bash, flush: true do %>

<% end %>

<%= render_snippet %>
<% end %>
<% end %>
</div>
