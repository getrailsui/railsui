<% content_for :demo_scripts do %>
<%= framework_cdn_script %>
<script>
// Tooltip examples
document.addEventListener('DOMContentLoaded', () => {
  const tooltipTriggerList = document.querySelectorAll('[data-bs-toggle="tooltip"]')
  const tooltipList = [...tooltipTriggerList].map(tooltipTriggerEl => new bootstrap.Tooltip(tooltipTriggerEl))
})
</script>
<% end %>

<% path = "railsui/themes/bootstrap/retriever/components/tooltips" %>

<%= render "#{path}/enabling" %>
<%= render "#{path}/usage" %>
<%= render "#{path}/directions" %>

<%= render "railsui/shared/read_more", subject: "tabs", link: "https://getbootstrap.com/docs/#{Railsui::Default::BOOTSTRAP_VERSION}/components/tooltips/" %>
