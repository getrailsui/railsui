<div aria-live="assertive" class="<%= container_classes %>" <%= html_attributes %>>
  <div class="<%= toast_classes %>">
    <div class="p-4">
      <div class="flex items-start">
        <!-- Icon -->
        <div class="shrink-0">
          <%= icon icon_name, class: icon_classes %>
        </div>

        <!-- Content -->
        <div class="ml-3 w-0 flex-1 pt-0.5">
          <p class="text-sm font-medium text-neutral-900 dark:text-white">
            <%= @title %>
          </p>
          <% if @message %>
            <p class="mt-1 text-sm text-neutral-500 dark:text-neutral-300">
              <%= @message %>
            </p>
          <% end %>
        </div>

        <!-- Dismiss Button -->
        <% if @dismissible %>
          <div class="ml-4 flex shrink-0">
            <button type="button"
                    class="inline-flex rounded-sm bg-white text-neutral-400 hover:text-neutral-500 focus:outline-hidden focus:ring-2 focus:ring-neutral-50 dark:bg-transparent dark:focus:ring-neutral-500 dark:hover:text-neutral-100 dark:focus:text-neutral-100"
                    data-action="click->railsui-toast#hide">
              <span class="sr-only">Close</span>
              <%= icon "x-mark", class: "size-5 pointer-events-none" %>
            </button>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>